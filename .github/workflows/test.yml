on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Test release
      uses: ultraio/action-gh-release@testing-1
      with:
        name: 'Test release'
        tag_name: testtag
        retries: 5
        retry_interval: 5000
        files: demo.png
